vcsn-dat: Operations on VCSN .dat (CSV) files
=============================================

Notes:
------

In vcsn2nc.R netCDF files are created in version 3 format by default.  Turning on compression results in netCDF version 4 output files.